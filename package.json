{
  "name": "reading-rating",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python -m http.server 4173",
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "engines": { "node": ">=20" }
}
